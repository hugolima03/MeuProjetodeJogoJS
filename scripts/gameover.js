class Gameover {
    constructor(gameoverImagem){
    this.gameoverImagem = gameoverImagem;
    }
    gameover(gameoverImagem){
        image=(this.gameoverImagem, 0, 0, windowWidth, windowHeight);
    }
    
}
